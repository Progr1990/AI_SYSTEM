name: Enterprise Release Pipeline

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout (pinned SHA)
        uses: actions/checkout@3df4ab11b1f...   # ← PODMIENIMY ZA CHWILĘ NA PRAWDZIWE SHA
        with:
          fetch-depth: 0

      - name: Verify Tag
        run: |
          set -euo pipefail
          git fetch --tags --force
          git rev-parse HEAD

      - name: Set up Python (pinned SHA)
        uses: actions/setup-python@82c7f2d...   # ← PODMIENIMY NA PRAWDZIWE SHA
        with:
          python-version: '3.11'

      - name: Install pinned dependencies
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip==23.3
          pip install cyclonedx-bom==3.12.0

      - name: Generate SBOM
        run: |
          set -euo pipefail
          cyclonedx-py -o sbom.json

      - name: Install Cosign (pinned)
        run: |
          set -euo pipefail
          COSIGN_VERSION=2.2.3
          curl -sSL -o cosign https://github.com/sigstore/cosign/releases/download/v${COSIGN_VERSION}/cosign-linux-amd64
          chmod +x cosign
          sudo mv cosign /usr/local/bin/

      - name: Build release artifact
        run: |
          set -euo pipefail
          mkdir dist
          cp -r spec dist/
          tar -czf enterprise-release.tar.gz dist

      - name: Sign artifact with Cosign (keyless OIDC)
        env:
          COSIGN_EXPERIMENTAL: "true"
        run: |
          set -euo pipefail
          cosign sign-blob \
            --yes \
            --output-signature enterprise-release.sig \
            enterprise-release.tar.gz

      - name: Publish GitHub Release (pinned SHA)
        uses: softprops/action-gh-release@8f4b7f84864484a7bf31766f...  # ← PODMIENIMY
        with:
          files: |
            enterprise-release.tar.gz
            enterprise-release.sig
            sbom.json